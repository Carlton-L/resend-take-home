// src/screens/SignInDemo/config.ts

/** The demo's own page, framed by the sign in screen. */
export const DEMO_PATH = '/demo';

/** The size the demo is drawn at, before the sign in screen scales it to its column. */
export const DEMO_WIDTH = 780;
export const DEMO_HEIGHT = 590;
