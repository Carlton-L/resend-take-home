// src/screens/SignInDemo/config.ts

/** The demo's own page, framed by the home page. */
export const DEMO_PATH = '/demo';

/** The size the demo is drawn at, before the home page scales it to its column. */
export const DEMO_WIDTH = 780;
export const DEMO_HEIGHT = 590;
